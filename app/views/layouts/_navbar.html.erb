<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "MYW", blog_posts_path, class: "navbar-brand", size: 50 %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><%= render 'layouts/demo' %></li>
        <% if signed_in? && current_user.role == "admin" %>
          <li><%= link_to "Manage Users", users_path %></li>
        <% end %>
        <% if signed_in? && current_user.role == "worker" %>
          <li><%= link_to "Schedule", schedule_path(id: current_user.id) %></li>
        <% end %>
        <% if signed_in? && current_user.role != "client" %>
          <li><%= link_to "Calender", jobs_path(current_time: Time.now) %></li>
        <% end %>
        <% if current_user %>
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
          <li><%= link_to current_user.username, edit_user_registration_path %></li>
        <% else %>
          <li><%= link_to "Sign Up", new_user_registration_path %></li>
          <li><%= link_to "Login", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
